export default function AdminRedirect() {
  return (
    <main style={{ padding: 24, fontFamily: "DM Sans, sans-serif" }}>
      <p>Admin tools live inside the Mini App Admin tab when you open the app as an admin user.</p>
    </main>
  );
}
